## SlackBot

Django app to manage a slack bot with support for custom message processors


Available settings:

* `SLACKBOT_BOT_TOKEN` - ...
* `SLACKBOT_APP_TOKEN` - ...
* `SLACKBOT_USER_MODEL` - ...

## Testing

...
